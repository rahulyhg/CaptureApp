angular.module('captureApp').controller('account_controller', ['$scope', '$routeParams', '$timeout', '$http', '$cookieStore',
    function ($scope, $routeParams, $timeout, $http, $cookieStore) {
    }]);